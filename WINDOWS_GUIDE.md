# Windows 使用说明

## 1. 安装

推荐下载 `PodcastRadar-Setup-*-x64.exe`。双击后按向导安装，开始菜单会出现 Podcast Radar；安装时也可以选择是否创建桌面图标。

如果不想安装，可下载 Portable ZIP，完整解压后运行 `Podcast Radar.exe`。不要只从压缩包里单独拖出 EXE，因为程序旁边的运行库也需要保留。

## 2. 发现节目

- “综合”汇总所有主题。
- “AI”侧重模型、算力、产品和产业新闻。
- “AI 创业”侧重创业者、产品形成、获客、融资与商业化故事。
- “天气”关注天气、农业、厄尔尼诺、拉尼娜和 ENSO。
- “养生”关注健康与生活方式。
- “人物监控”单独跟踪 Dylan Patel、Elon Musk 等重点人物的最新访谈。

切换栏目后，当前栏目按钮会变黑；刷新期间会显示状态条和滑板小恐龙。

## 3. 下载与转录

1. 在节目卡片中点击“选择”。
2. 确认右侧节目信息、原链接和音频状态。
3. 点击“确认转译”进入下载和 ASR 队列。
4. 默认先尝试本地 Whisper；首次运行要下载模型，后续会复用缓存。
5. 完成后可在“阅读”和“资料库”查看逐字稿、纪要与译文。

## 4. 配置阿里云 API

如果希望把转录切换到线上：

1. 打开“设置”。
2. 在 DashScope API Key 输入框填写自己的 Key。
3. 保存后，把转录模式切换为“极速云端”。

API Key 仅保存在本机：

```text
%LOCALAPPDATA%\PodcastRadar\.env
```

请勿把该文件发给别人，也不要提交到 GitHub。

## 5. 绑定自己的本地 Whisper

Windows 安装包已经包含 `faster-whisper` 运行代码。模型由程序在第一次使用时下载到 Hugging Face 缓存。高级用户可以设置 `HF_HOME` 或 `HF_HUB_CACHE`，让程序复用已有模型缓存。

当前默认配置面向普通 CPU，优先稳定与兼容性。若自行改源码，可在 `app/transcription.py` 中调整模型、设备和量化方式。

## 6. 可选的本地中文朗读

“播放译文”使用外部 Qwen3-TTS 环境。默认查找：

```text
%USERPROFILE%\qwen3_tts_local\.venv\Scripts\python.exe
```

未安装该环境不会影响节目发现、转录、翻译和阅读功能；也可以在设置页指定其他兼容 Python 路径。

## 7. 数据位置与备份

下载、设置、研究资料和人物监控数据位于：

```text
%LOCALAPPDATA%\PodcastRadar
```

需要迁移电脑时，退出应用后复制整个目录即可。程序卸载器默认保留此目录，避免误删研究资料。

## 8. 常见问题

### 首次转录较慢

通常是 Whisper 模型第一次下载。确认网络可访问 Hugging Face，等待完成后再判断性能。

### 某个节目无法转录

先点击“打开原文”确认节目仍存在。部分网页只有文字或视频页面，没有可用音频；程序会在详情区提示媒体状态。

### Windows SmartScreen 提示

当前为开源测试版，尚无商业代码签名。只从本仓库 Release 下载，并使用 `SHA256SUMS.txt` 核对文件哈希。
